export default function Resize() {
    return (
      <canvas
        style={{
          "border": "1px solid",
          width: window.innerWidth,
          height: window.innerHeight

        }}
      ></canvas>
     
    );
  }
  
